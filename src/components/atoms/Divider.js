import styled from "@emotion/styled";

export const Divider = () => {
  return <SDivider></SDivider>;
};

const SDivider = styled.div`
  width: 45%;
  margin: 0 auto;
  border-bottom: 1px solid #333;
  opacity: 0.1;
  margin-bottom: 10vh;
`;
